// Local files imports.
const statusCodes = require('./statusCodes');
const messages = require('./messages');

const configs = {
    statusCodes,
    messages,
};

// Exports.
module.exports = configs;
